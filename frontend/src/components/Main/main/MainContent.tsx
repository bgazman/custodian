import React from 'react';




const MainContent = ({ children, className = '' }) => {
    return (
            <div>
                {children}
            </div>
    );
};

export default MainContent;